version: "3.7"
services:
  derby:
    image: opavlova/db-derby
    ports:
      - "1024:1527"
    restart: unless-stopped

  tomcat:
    image: com.webapp.gessi/gessi-slr:1.0.1
    depends_on:
      - derby
    ports:
      - "1025:8080"

networks:
  default:

volumes:
  db-volume:
    external: true